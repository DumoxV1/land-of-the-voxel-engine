{
  "video_id": "QFQkqFSg8Z4",
  "segment_count": 367,
  "duration": "14:16",
  "full_text": "So, before anyone starts typing any heated comments, I admit it. The title is clickbait. For those who don't know, Rayon is a popular multi-threading library for Rust, and it's arguably quite useful. The library is mature, meticulously tested, and easy to set up. But, the design choices behind Rayon don't align well with the real-time needs of video games. So, in this video, I want to talk about those limitations and how and why I built my own custom lock-free thread pool for my voxel game engine. CPUs nowadays come with many cores. This means that your computer can run multiple pieces of code or threads at the exact same time. Rayon makes it easy to distribute work across those threads. For example, this program iterates over a bunch of numbers and calculates their square roots. By changing the iterator to one of Rayon's parallel iterators, the loop body will run on multiple threads, making the whole program faster. This style of multi-threading is super simple and expressive. There's no need to schedule callbacks or worry about synchronization barriers or anything like that. You just write code that looks synchronous, but with parallel loops. Rayon also allows you to spawn background tasks like this, which do run asynchronously. [music] Now, in my game engine, there are many places where this is handy. World generation, calculating acceleration structures, physics, [music] and so forth can all be parallelized. Initially, I was using Rayon for this, until I noticed the lag spikes. What was happening was that the main thread would need some parallel work done immediately, like the physics, and all of the background threads would be busy with world generation, and this would cause a stall. And so this is the systemic issue with Rayon that I do want to address, which is that it's designed for [music] throughput, not latency. Throughput is the average amount of data your code processes over time. The higher your program's throughput, the faster it is. Latency, on the other hand, is how quickly [music] your code can get to an answer for a particular call. So if the main thread schedules 5 milliseconds of physics work, but then waits 20 milliseconds while Rayon is busy with other stuff, that's 20 milliseconds of added latency on the main thread. Let me be a bit more specific about how this works. At startup, Rayon creates a fixed thread pool of workers. When an external thread, like the main thread, wants to run a parallel iterator, it puts that iterator into one of the workers' queues. When the worker thread is ready, it will begin processing that iterator. And if other worker threads are free, they will work steal and grab parts of that same iterator to process themselves. Once the whole thing is done, control returns to the main thread. Asynchronous background tasks are also added to the worker queues in the same way. The biggest issue here is that the external thread doesn't participate in the work. If the workers are busy, there's no bound, time-wise, on how long the main thread waits for its work to begin. It's also extremely likely that a context switch occurs whenever the main thread schedules work. The OS will swap the main thread out for something else, which is wasteful. For a command-line application where you're just churning through data, that doesn't matter. All of your threads are busy, so you're getting maximum throughput anyway. For games, though, it can be the difference between a smooth and stuttery experience. To make this more concrete, here's my physics engine simulating 1,000 boxes. With Rayon, once the boxes settle, uh the physics would take 12 milliseconds on average per tick. And here's the same thing in a profiling tool. So, on the horizontal axis here, we have time. And then, on axis we have each thread. So, these colored boxes indicate what the threads were doing over time. And so, [music] the main thread, which is the top one here, starts out by beginning the physics step. It does some initialization >> [music] >> and sorts all of the objects into islands. And because most of the boxes are touching in this example scene, most of them end up in this one big island. Anyway, after the main thread is done with its early work, it dispatches all of the islands to the workers, so the workers can simulate them in parallel. [music] And so, we can see that over the course of this particular tick, all five of my worker threads picked up and ran a bunch of small islands. And worker number two got stuck with this really long task to simulate the big island. Um this So, this isn't bad, 12 milliseconds per tick. But, one thing is immediately obvious, which is, as I was saying before, the main thread is just sitting [music] idle this entire time. It's waiting for the islands that it sent to the workers to finish running. Now, you might argue that one way to sidestep this is to just add another worker to the thread pool, so that it can run instead of the main thread. But, the problem with that is the the main thread might want to go off and do other things while the workers are running. So, if you add any more threads here, you'll end up um potentially oversaturating the CPU because all [music] of the workers and the main thread try to run at the same time, and that will lead to lag. The CPU won't be able to run all of the threads at once. One other thing to note is that sometimes workers will pick up completely unrelated tasks while they're processing. So, for example, in this particular uh island task [music] that worker number three had, it began by running collision detection, and then it had some parallel work to do. [music] It wanted to do graph coloring and um mask calculations at the same time. And so, it began with the graph coloring, and then engine number one jumped in to help with the mask calculations. And so, engine worker number three was left waiting for those mask calculations to finish. And so, it began grabbing just other tasks and running them. Now, this I know I've been talking a lot about not letting threads sit idle. So, this is good except for the fact that Rayon doesn't distinguish between which task gets picked up when. So, instead of these being simulate island tasks, they could have been like a world generation task that would take 50 milliseconds to complete, and then you'd have a stall. All right. So, that was the problem. But, what did I do about it? What do you think we do on this channel? I rewrote Rayon from scratch, of course. My library is called micropool. Check it out. It's got the join primitive. It's got background tasks, and it even supports parallel iterators using the paralite library. But, what makes this different? First off, whenever a thread runs a parallel iterator, that thread actively helps to complete the work, even if it's an external thread. So, this means that the main thread doesn't sit idle. And beyond that, the library remembers which external thread originally spawned a given task and any subtasks or sub iterators. If a worker is waiting for the results of a parallel iterator, it might grab some other work, but it will only ever grab work that was spawned from the same original root task. So, this prevents time-sensitive stuff, like physics, from getting stalled because a thread decides to do world gen while it waits. The coolest part about this, though, is that the scheduler is lock-free. Pretty much everything is done with atomics. Multiple threads can spawn new tasks and look for available work simultaneously. And this wasn't how I started out. Initially, I had a much more traditional job system, which had a tree of jobs behind a read-write lock. But, when I looked at the results in the profiler, it looked like this. See all of these orange sections? That's very bad. They indicate contention. Threads waiting for the lock so that they don't interfere. And this problem just gets worse and worse with the more threads you use. So, this was what made me say, \"I got to go lock-free.\" So, to round things off, I thought we'd do a tiny little code walk and see how this actually works in micropool. When a thread goes to schedule a parallel iterator, that parallel iterator becomes a job. And each job has one or more work units. For an iterator, a work unit could be like one element in the iterator, or more likely it'll be a group of elements that the background threads process together. So, each work unit is processed by exactly one thread. The information for each job is stored in this job structure, and this is really the heart of micro pool. The job structure has an atomic counter, which tracks the number of work units that haven't been started for the job, and then it has a pointer to additional information. Um and this additional information includes a function to call to actually run work units, and a tracker that remembers how many work units have not finished yet. So, when a parallel iterator is scheduled, it'll fill out one of these job slots, and micro pool maintains a fixed array of these slots. Um so, it'll fill out a job slot, and then as background threads are going around looking for work to do, they'll just go through the array, and they will decrement uh they'll subtract one from this available units counter, and if that fetch sub atomic operation returns a positive value, then the worker thread knows it has reserved a work unit. Uh whereas if this returns a negative value, it indicates that all of the jobs have all of the units for the job have already been taken, or there was no valid job at all. If a worker thread has successfully reserved a job, it will then go ahead and call this um function to run work units. And once all of the available work units are exhausted, it will subtract the number of work units it completed from this incomplete units counter, which tracks how many units have yet to finish. Once this counter hits zero, uh that's how the main thread knows that the parallel iterator is completely done, and it can move on. The only other uh thing to make note of here is that there's some bookkeeping involving which job slots are taken and which job slots are available, and also tracking the root task um that's responsible for any job. So, all of that is done with this atomic bit structure. >> [music] >> And what this is is it's just a big array of booleans that have been bit packed into integers. And so, this structure allows for um setting elements as well as efficiently um iterating through the ones and the zeros. And this is possible because you can read a single 64-bit integer and then look at the 64 booleans contained within it. When searching for work [music] to complete, worker threads will look at an atomic bit mask, and they'll only choose jobs that are marked within that bit [music] mask. And so, this helps separate time-critical stuff, like the physics, from just background tasks, like the world generation. There's really not much more to the architecture here. The real challenge with this library is dealing [music] with all of the details of atomic orderings and the lifetime invariance, and making sure that threads go to sleep and wake up at the appropriate times. Nonetheless, the library is unit tested, and I've run everything through [music] the Miri sanitizer without any problems, so it works great. Here we are back in Tracy, but this time with micro pool. The average time it takes for that boxing from before um is now down to 8 milliseconds over the original 12 milliseconds with Rayon. So, I was really ecstatic when I saw this uh because just by swapping out the thread pool library, I was able to make my program much faster. Like Rayon was 50% slower >> [music] >> um than micro pool in this case. And the first thing to notice is that the main thread is participating in helping to complete work. It's grabbing jobs from some of the background [music] threads, especially this big green thread. It's helping out with a lot of the collision detection and solving. And all of these smaller jobs are running independently. [music] None of them are picking up world generation tasks or other things that might cause lag. And now my voxel engine runs buttery smooth. That brings us to the end of the video. I know this was more technical than usual, so please leave a comment down below letting me know what you think. Otherwise, don't forget to like and subscribe and have a lovely day.",
  "timestamped_text": "0:00 So, before anyone starts typing any\n0:02 heated comments, I admit it. The title\n0:04 is clickbait. For those who don't know,\n0:07 Rayon is a popular multi-threading\n0:09 library for Rust, and it's arguably\n0:12 quite useful. The library is mature,\n0:15 meticulously tested, and easy to set up.\n0:18 But, the design choices behind Rayon\n0:21 don't align well with the real-time\n0:23 needs of video games.\n0:25 So, in this video, I want to talk about\n0:27 those limitations and how and why I\n0:30 built my own custom lock-free thread\n0:33 pool for my voxel game engine.\n0:36 CPUs nowadays come with many cores.\n0:39 This means that your computer can run\n0:41 multiple pieces of code or threads at\n0:44 the exact same time.\n0:47 Rayon makes it easy to distribute work\n0:49 across those threads.\n0:51 For example, this program iterates over\n0:54 a bunch of numbers and calculates their\n0:56 square roots.\n0:57 By changing the iterator to one of\n0:59 Rayon's parallel iterators, the loop\n1:02 body will run on multiple threads,\n1:05 making the whole program faster. This\n1:07 style of multi-threading is super simple\n1:09 and expressive.\n1:11 There's no need to schedule callbacks or\n1:13 worry about synchronization barriers or\n1:15 anything like that.\n1:17 You just write code that looks\n1:19 synchronous, but with parallel loops.\n1:22 Rayon also allows you to spawn\n1:23 background tasks like this, which do run\n1:27 asynchronously. [music]\n1:29 Now, in my game engine, there are many\n1:31 places where this is handy.\n1:34 World generation, calculating\n1:36 acceleration structures, physics,\n1:38 [music] and so forth can all be\n1:40 parallelized.\n1:42 Initially, I was using Rayon for this,\n1:45 until I noticed the lag spikes.\n1:48 What was happening was that the main\n1:49 thread would need some parallel work\n1:51 done immediately, like the physics,\n1:54 and all of the background threads would\n1:55 be busy with world generation, and this\n1:58 would cause a stall.\n1:59 And so this is the systemic issue with\n2:02 Rayon that I do want to address, which\n2:04 is that it's designed for [music]\n2:05 throughput, not latency.\n2:08 Throughput is the average amount of data\n2:10 your code processes over time.\n2:13 The higher your program's throughput,\n2:15 the faster it is.\n2:17 Latency, on the other hand, is how\n2:19 quickly [music] your code can get to an\n2:21 answer for a particular call.\n2:24 So if the main thread schedules 5\n2:26 milliseconds of physics work, but then\n2:28 waits 20 milliseconds while Rayon is\n2:30 busy with other stuff, that's 20\n2:32 milliseconds of added latency on the\n2:34 main thread.\n2:36 Let me be a bit more specific about how\n2:39 this works.\n2:40 At startup, Rayon creates a fixed thread\n2:43 pool of workers.\n2:45 When an external thread, like the main\n2:48 thread, wants to run a parallel\n2:50 iterator,\n2:51 it puts that iterator into one of the\n2:53 workers' queues.\n2:55 When the worker thread is ready, it will\n2:58 begin processing that iterator. And if\n3:00 other worker threads are free, they will\n3:03 work steal and grab parts of that same\n3:06 iterator to process themselves.\n3:09 Once the whole thing is done, control\n3:11 returns to the main thread.\n3:14 Asynchronous background tasks are also\n3:16 added to the worker queues in the same\n3:18 way.\n3:19 The biggest issue here is that the\n3:21 external thread doesn't participate in\n3:23 the work.\n3:25 If the workers are busy, there's no\n3:27 bound, time-wise, on how long the main\n3:30 thread waits for its work to begin.\n3:33 It's also extremely likely that a\n3:35 context switch occurs whenever the main\n3:37 thread schedules work. The OS will swap\n3:40 the main thread out for something else,\n3:42 which is wasteful.\n3:44 For a command-line application where\n3:46 you're just churning through data, that\n3:48 doesn't matter. All of your threads are\n3:50 busy, so you're getting maximum\n3:51 throughput anyway.\n3:53 For games, though, it can be the\n3:54 difference between a smooth and stuttery\n3:57 experience.\n3:59 To make this more concrete, here's my\n4:02 physics engine simulating 1,000 boxes.\n4:05 With Rayon, once the boxes settle,\n4:08 uh the physics would take 12\n4:09 milliseconds on average per tick. And\n4:12 here's the same thing in a profiling\n4:14 tool.\n4:15 So, on the horizontal axis here, we have\n4:18 time. And then, on axis we have each\n4:21 thread. So, these colored boxes indicate\n4:24 what the threads were doing over time.\n4:28 And so, [music]\n4:29 the main thread, which is the top one\n4:30 here, starts out by beginning the\n4:32 physics step. It does some\n4:34 initialization\n4:35 >> [music]\n4:35 >> and sorts all of the objects into\n4:37 islands. And because most of the boxes\n4:39 are touching in this example scene, most\n4:42 of them end up in this one big island.\n4:45 Anyway, after the main thread is done\n4:48 with its early work, it dispatches all\n4:51 of the islands to the workers, so the\n4:54 workers can simulate them in parallel.\n4:56 [music]\n4:56 And so, we can see that over the course\n4:59 of this particular tick,\n5:01 all five of my worker threads\n5:04 picked up and ran a bunch of small\n5:06 islands. And worker number two\n5:09 got stuck with this really long task to\n5:12 simulate the big island.\n5:16 Um this So, this isn't bad, 12\n5:18 milliseconds per tick.\n5:20 But, one thing is\n5:23 immediately obvious, which is, as I was\n5:25 saying before, the main thread is just\n5:29 sitting [music] idle this entire time.\n5:31 It's waiting for the islands that it\n5:33 sent to the workers to finish running.\n5:36 Now, you might argue that one way to\n5:40 sidestep this is to just add another\n5:42 worker to the thread pool, so that it\n5:44 can run instead of the main thread.\n5:46 But, the problem with that is the the\n5:49 main thread might want to go off and do\n5:51 other things\n5:52 while the workers are running. So, if\n5:54 you add any more threads here,\n5:57 you'll end up um\n5:59 potentially oversaturating the CPU\n6:01 because all [music] of the workers and\n6:03 the main thread try to run at the same\n6:05 time, and that will lead to lag. The CPU\n6:08 won't be able to run all of the threads\n6:10 at once. One other thing to note is that\n6:12 sometimes workers will pick up\n6:14 completely unrelated tasks while they're\n6:17 processing. So, for example,\n6:20 in this particular\n6:22 uh island task [music] that worker\n6:24 number three had,\n6:26 it began by running collision detection,\n6:28 and then it had some parallel work to\n6:30 do. [music] It wanted to do graph\n6:32 coloring and um mask calculations at the\n6:35 same time. And so, it began with the\n6:37 graph coloring, and then engine number\n6:39 one jumped in to help with the mask\n6:41 calculations.\n6:43 And so, engine worker number three was\n6:45 left waiting\n6:47 for those mask calculations to finish.\n6:50 And so, it began grabbing just other\n6:52 tasks and running them.\n6:55 Now, this\n6:56 I know I've been talking a lot about not\n6:58 letting threads sit idle. So, this is\n7:01 good except for the fact that Rayon\n7:03 doesn't distinguish between\n7:06 which task gets picked up when. So,\n7:09 instead of these being simulate island\n7:12 tasks, they could have been like a world\n7:14 generation task that would take 50\n7:16 milliseconds to complete, and then you'd\n7:18 have a stall.\n7:20 All right. So, that was the problem.\n7:22 But, what did I do about it?\n7:25 What do you think we do on this channel?\n7:27 I rewrote Rayon from scratch, of course.\n7:30 My library is called micropool. Check it\n7:32 out. It's got the join primitive. It's\n7:34 got background tasks, and it even\n7:36 supports parallel iterators using the\n7:38 paralite library.\n7:40 But, what makes this different?\n7:43 First off, whenever a thread runs a\n7:45 parallel iterator, that thread actively\n7:47 helps to complete the work, even if it's\n7:49 an external thread. So, this means that\n7:52 the main thread doesn't sit idle.\n7:55 And beyond that, the library remembers\n7:58 which external thread originally spawned\n8:00 a given task and any subtasks or sub\n8:03 iterators. If a worker is waiting for\n8:06 the results of a parallel iterator, it\n8:09 might grab some other work, but it will\n8:11 only ever grab work that was spawned\n8:13 from the same original root task.\n8:16 So, this prevents time-sensitive stuff,\n8:18 like physics,\n8:20 from getting stalled because a thread\n8:21 decides to do world gen while it waits.\n8:25 The coolest part about this, though, is\n8:27 that the scheduler is lock-free.\n8:29 Pretty much everything is done with\n8:31 atomics.\n8:32 Multiple threads can spawn new tasks and\n8:35 look for available work simultaneously.\n8:38 And this wasn't how I started out.\n8:40 Initially, I had a much more traditional\n8:42 job system, which had a tree of jobs\n8:44 behind a read-write lock.\n8:47 But, when I looked at the results in the\n8:48 profiler, it looked like this.\n8:51 See all of these orange sections? That's\n8:53 very bad. They indicate contention.\n8:56 Threads waiting for the lock so that\n8:58 they don't interfere.\n9:00 And this problem just gets worse and\n9:01 worse with the more threads you use. So,\n9:04 this was what made me say, \"I got to go\n9:06 lock-free.\"\n9:08 So, to round things off, I thought we'd\n9:10 do a tiny little code walk and see how\n9:12 this actually works in micropool.\n9:16 When a thread goes to schedule a\n9:18 parallel iterator, that parallel\n9:20 iterator becomes a job. And each job has\n9:24 one or more work units. For an iterator,\n9:28 a work unit could be like one element in\n9:31 the iterator, or more likely it'll be a\n9:33 group of elements that the background\n9:36 threads process together. So, each work\n9:38 unit is processed by exactly one thread.\n9:43 The information for each job is stored\n9:46 in this job structure, and this is\n9:47 really the heart of micro pool. The job\n9:50 structure has an atomic counter,\n9:53 which tracks the number of work units\n9:55 that haven't been started for the job,\n9:57 and then it has a pointer to additional\n9:59 information. Um and this additional\n10:01 information includes a function to call\n10:04 to actually run work units,\n10:07 and a tracker that remembers how many\n10:10 work units have not finished yet.\n10:13 So, when a parallel iterator is\n10:16 scheduled,\n10:17 it'll fill out one of these job slots,\n10:20 and micro pool maintains a fixed array\n10:24 of these slots.\n10:25 Um so, it'll fill out a job slot, and\n10:27 then as background threads are going\n10:29 around looking for work to do, they'll\n10:31 just go through the array, and they will\n10:34 decrement uh they'll subtract one from\n10:37 this available units counter, and if\n10:40 that fetch sub atomic operation returns\n10:43 a positive value,\n10:45 then the worker thread knows it has\n10:48 reserved a work unit. Uh whereas if this\n10:50 returns a negative value, it indicates\n10:52 that all of the jobs have all of the\n10:55 units for the job have already been\n10:56 taken, or there was no valid job at all.\n10:59 If a worker thread has successfully\n11:01 reserved a job,\n11:03 it will then go ahead and call this um\n11:06 function to run work units. And once all\n11:10 of the available work units are\n11:11 exhausted, it will subtract the number\n11:14 of work units it completed from this\n11:17 incomplete units counter, which tracks\n11:19 how many units have yet to finish.\n11:21 Once this counter hits zero, uh that's\n11:24 how the main thread knows that the\n11:27 parallel iterator is completely done,\n11:29 and it can move on.\n11:32 The only other uh thing to make note of\n11:34 here is that there's some bookkeeping\n11:36 involving which job slots are taken and\n11:39 which job slots are available, and also\n11:42 tracking the root task um that's\n11:46 responsible for any job. So, all of that\n11:48 is done with this atomic bit structure.\n11:51 >> [music]\n11:51 >> And what this is is it's just a big\n11:53 array of booleans that have been bit\n11:56 packed into integers.\n11:58 And so, this structure allows for um\n12:02 setting elements as well as efficiently\n12:06 um iterating through the ones and the\n12:09 zeros. And this is possible because you\n12:11 can read a single 64-bit integer and\n12:14 then look at the 64 booleans contained\n12:17 within it.\n12:18 When searching for work [music] to\n12:19 complete, worker threads will look at an\n12:22 atomic bit mask, and they'll only choose\n12:24 jobs that are marked within that bit\n12:26 [music] mask. And so, this helps\n12:27 separate\n12:28 time-critical stuff, like the physics,\n12:31 from just background tasks, like the\n12:33 world generation.\n12:35 There's really not much more to the\n12:37 architecture here.\n12:39 The real challenge with this library is\n12:42 dealing [music] with all of the details\n12:43 of atomic orderings and the lifetime\n12:46 invariance, and making sure that threads\n12:48 go to sleep and wake up at the\n12:50 appropriate times.\n12:52 Nonetheless,\n12:53 the library is unit tested, and I've run\n12:56 everything through [music] the Miri\n12:58 sanitizer without any problems, so it\n13:01 works great. Here we are back in Tracy,\n13:04 but this time with micro pool. The\n13:06 average time it takes for that boxing\n13:09 from before um is now down to 8\n13:12 milliseconds over the original 12\n13:14 milliseconds with Rayon. So, I was\n13:17 really ecstatic when I saw this uh\n13:19 because just by swapping out the thread\n13:21 pool library, I was able to make my\n13:23 program much faster. Like Rayon was 50%\n13:26 slower\n13:26 >> [music]\n13:27 >> um than micro pool in this case.\n13:29 And the first thing to notice is that\n13:31 the main thread is participating\n13:34 in helping to complete work. It's\n13:37 grabbing jobs from some of the\n13:39 background [music] threads, especially\n13:40 this big green thread. It's helping out\n13:42 with a lot of the collision detection\n13:44 and solving. And all of these smaller\n13:47 jobs are running independently. [music]\n13:50 None of them are picking up\n13:52 world generation tasks or other things\n13:55 that might cause lag. And now my voxel\n13:57 engine runs buttery smooth.\n14:01 That brings us to the end of the video.\n14:03 I know this was more technical than\n14:05 usual, so please leave a comment down\n14:07 below letting me know what you think.\n14:09 Otherwise, don't forget to like and\n14:11 subscribe and have a lovely day."
}
