{
  "video_id": "ztkh1r1ioZo",
  "segment_count": 138,
  "duration": "15:29",
  "full_text": "Today we're reinventing voxel ray traversal from \nscratch to be able to render over 32 billion   voxels in real time. Most voxel games, like \nMinecraft, convert their worlds into triangle   meshes to render them. To me, this has always \nfelt like a compromise: we waste time, memory,   and complexity converting perfectly good voxel \ndata into triangles because GPUs are optimized   for triangle rasterization. But what if we \ncould render the world directly — no triangles,   no meshing — and somehow do it fast enough for \nreal time? That's exactly what we'll do today.   We’ll go from the basics of what a voxel even is \nto an algorithm capable of rendering billions of   voxels in real time by casting a ray for each \npixel on the screen using the GPU. And towards   the end of the video, I'll show you one simple \noptimization technique that 10x'd the FPS. So,   how do we go from a bunch of voxels and a camera \nto pixels on a screen? Well, to answer that,   we need to start at the very beginning — \"What is \na Voxel?\" The screen you're watching this video on   is made up of millions of physical pixels, behind \neach of which is a logical pixel — a simple piece   of data that determines its color. This is \noften an RGB color but it doesn't have to be;   it could just as well be a single luminance value \nor literally anything else, and it would still be   a pixel. In my opinion, the only thing required \nfor something to be a pixel is that it's a small   building block of 2D space. In the same way we \ncan imagine 3D space as being made up of so-called   voxels. For simplicity, my voxels will each be a \nsingle bit of data: on or off, one or zero, true   or false, solid or empty. But, like pixels, voxels \ncan store any kind of data you want. For example,   each voxel could hold an integer representing \na material type like air, stone, grass, dirt,   etc., etc. But the idea of a grid of voxels itself \ndoesn't help us a lot unless we can efficiently   read and write it to memory. Therefore, we’ll \nuse this flattening function to convert a 3D   coordinate to a 1D index, allowing us to \nstore the voxel data in an array of size   width times depth times height. You can think \nof this function as taking each layer along   the y-axis and placing them side by side along the \nx-axis, and then taking each row along the z-axis   and placing them end to end. There are other \nmethods that preserve spatial locality better to   improve memory performance, or ones that compress \nthe voxel data to use less memory overall,   but I'll talk more about those towards the end of \nthe video. Going forward, the visualizations will   be in 2D and the math and algorithms will be in \n3D but all of it works in any dimension you want   with only a few minor modifications. Finally, we \nneed to be able to convert an arbitrary position   in space to the corresponding voxel coordinates \nand for that we’ll just take the floor of each   component. If this position has the possibility of \nbeing outside of the grid, due to numerical errors   or actually being outside, we'll have to clamp it \nor check it to make sure we don't access out of   bounds. Now that we know what a voxel is, how do \nwe describe the idea of a pixel looking at one?   That’s where the mathematical concept of a ray \ncomes in — which leads us to the next question:   \"What is a Ray?\" In our case it will make the \nmost sense to define a ray, given by the origin   O and the non-zero direction D, as the set of all \npoints P that satisfy these two constraints. In   other words, all points from the given origin \nalong the given direction. The variable t is   a parameter that tells us how many multiples \nof the direction we've moved from the origin.   Note that nowhere in this definition do we say \nthat the direction is normalized. It can be   whatever length you want as long as it's not zero. \nSo given a ray corresponding to some pixel we need   to figure out if it hits a voxel — and if so, \nwhich one. The idea is to walk through the voxel   grid along that ray, in increasing values of t, \nuntil we either find the first solid one or exit   the bounds of the grid. This is what’s known \nas voxel ray traversal. Now that we know the   idea behind what voxel ray traversal is, we can \nstart to think about how we would implement it.   Let’s begin with a naive method, because every \ngood computer science problem starts with   something painfully slow and completely wrong. In \nthis case, that would be to just start at the ray   origin stepping forward in small delta t multiples \nof the direction, checking for a solid voxel at   each step. This works but it has one major issue; \nit can completely miss voxels which the ray should   intersect with. And if we try to minimize the \nrisk of this by decreasing the step size, we   could spend hundreds or thousands of iterations on \na single voxel which is very slow, not to mention   that it doesn't even fix the issue. So let's take \na step back and think about the problem again.   We have a grid and a ray and we want to determine \nwhich voxels the ray intersects with in order. So   let's start simple; how do we find the first \nvoxel? If the origin is inside of the grid, we   know that the first voxel is just the floor of the \nray origin, but if it's outside, we somehow need   to calculate where the ray enters the axis-aligned \nbounding box around the grid. For this we'll need   a Ray-AABB intersection algorithm. To do this we \nwill take the mathematical definition of a ray and   the mathematical definition of an AABB and solve \nfor the ray parameter t. After working through   the math and handling some edge-cases we arrive \nat this algorithm, known as the slab method for   Ray-AABB intersection. Now, because it's almost \nalways faster to multiply than to divide, it makes   sense to precompute one over the direction and \nstore that in our ray struct. And then multiply   by that instead of divide by the direction. From \nthe Ray-AABB intersection algorithm we get the   values of t when the ray enters and exits the \nbounding box. If we don't hit the bounding box,   we know that we won't hit a single voxel in the \ngrid either so we can return early. Otherwise we   use the first t-value with the ray definition to \ncalculate the point of entry. Since this point has   the chance of being slightly outside of the grid \ndue to floating-point errors, we'll need to use   the clamping version when we get the corresponding \nvoxel coordinate. Now that we know how to get the   first voxel that the ray intersects with, the only \nthing we need for a complete voxel ray traversal   algorithm is a way to get the next voxel since \nthen we can just do that in a loop until we hit   a solid voxel or go out of bounds. So given a ray \nwhich we know intersects with a given voxel, how   do we get the next voxel that the ray intersects \nwith? Well, we can imagine the voxel grid as being   made up of a stack of axis-aligned planes along \neach axis. The ray travels through these planes,   and whenever it crosses one of them, we enter \na new voxel. So, to find the next voxel,   all we need to do is figure out which of these \nplanes the ray will exit the voxel through. The   lesser plane on each axis, just has the positions \nof the components of v and the greater planes,   the components of v plus one. First of all, we can \nnotice that if the x-component of the direction   is positive we can never exit through the lesser \nx-plane, and if it's negative we can never exit   through the greater x-plane. This applies to y and \nz too which brings us down to one plane per axis   which we can determine the positions of based on \nthe sign of the direction. Because of some quirks   of floating-point zero we'll actually want to use \nthe sign of the inverse of the direction instead   of the sign of the direction directly. Now that \nwe know the position of the planes, we just need   to calculate which plane the ray intersects with \nfirst. So let's focus on the x-plane. Since the   x-plane is defined as being all points for \nwhich the x-coordinate is a certain value,   let's call it x_0, and the ray is defined as all \npoints that satisfy this equation, we just have to   solve for the value of t when the x-component of \nP is equal to x_0. This is just a linear equation   and by solving it we get this, and I'll call it \nt_x because it's the t-value to hit the x-plane.   Of course, we could just as well have done this \nfor the y-axis or the z-axis and the derivation   would've been the same. Now that we have the \nthree t-values, we just have to compare them to   see which one occurs first. From the sign of the \ndirection, we also know if we're supposed to step   in the positive or negative direction, in other \nwords, to get the next voxel in the traversal   we want to add the sign of the direction to the \ncomponent of the voxel coordinate, for which the   plane intersection t-value is lowest. And again \nfor the same reason, we'll use the sign of the   inverse instead. Putting it all together, we first \ncalculate the initial voxel using the Ray-AABB   intersection algorithm. Then we repeatedly step \nto the next voxel by comparing the t-values for   the plane intersections and if we continue this \nprocess until we hit a solid voxel or exit the   bounds of the grid, we will have traversed every \nintersected voxel. One quick improvement. We can   see that each iteration, coord is increased by \nthe sign of the direction which means that t   is increased by the sign of the direction times \nthe inverse of the direction, in other words the   absolute value of the inverse of the direction. \nTherefore, we can pull out the entirety of t and   increment it each iteration instead. The algorithm \nthat we now have is essentially the one described   by Amanatides and Woo in their 1987 paper 'A Fast \nVoxel Traversal Algorithm for Ray Tracing'. Now   let's use the algorithm to render something and \nfor that, we of course need something to render.   Therefore, I've chosen the Stanford Bunny, a model \nwhich is very popular in computer graphics. The   Stanford Bunny is a triangle mesh so we'll need to \nvoxelize it which I won't cover how to do in this   video but the algorithm is included in the GitHub \nrepository in the description below along with a   bare-bones implementation of this video's topic in \nRust and Vulkan. And now if we set up a camera and   shoot a ray for each pixel using a compute shader \nrunning the algorithm, we get this. Right now,   the traversal only tells us whether we miss or \nwhich voxel we hit. But with a bit of extra math,   we can efficiently compute which face of the voxel \nwe hit, where on the face we hit, and how far from   the camera it happened. This gives us everything \nwe need for basic shading and texturing, or even   launching secondary rays for ray tracing. Now it's \ntime for that one optimization that I alluded to   at the start of the video. Currently, at a grid \nresolution of 2048 cubed and a screen resolution   of 1000 by 1000 we're getting about 12 FPS. \nAfter this optimizations, we'll hit roughly 121   FPS — which is a 10x speedup. But before we get to \nthat, I want to cover an alternative optimization   which is almost as good but way easier to explain. \nIn the \"What is a Voxel?\" section of this video,   we covered the flattening function and I \nmentioned that there are other ways of doing it.   It turns out that if we switch to this one, \nknown as a Z-order curve, we get 102 FPS which   is already over 8 times faster. You might be \nthinking, \"Wait, we just made the flattening   function way more complicated, and it got faster?\" \nYeah! The reason why is that with a Z-order curve,   we place voxels that are nearby in space nearby \nin memory. This has two main implications on the   GPU. First of all, since our voxel ray traversal \nalways steps from one voxel to a neighboring one,   this improves the likelihood that the next voxel \nis already in the cache. And second of all, it   improves the likely hood that two nearby rays want \nto access the same memory meaning that the GPU   doesn't have to read as much data into cache. All \nin all, this means we spend less time waiting for   memory to read and more time actually traversing. \nNow like I said before, there's actually a better   way to do this. Since we're doing the traversal on \nthe GPU, we have access to GPU specific structures   like 3D textures which are heavily optimized for \ndata locality using way more sophisticated methods   than Z-order curves. Switching to storing our \nvoxels in a 3D image brings us to 121 FPS which is   a 10 times improvement over the original 12. So by \nrethinking how the voxel data is stored in memory,   first with a Z-order curve and then a 3D texture, \nwe brought our FPS from 12 to 121 without even   changing the algorithm itself. So there you \nhave it, from the definitions of voxels,   rays and traversals, to a complete real-time \nrendering pipeline with zero triangles, capable   of rendering billions of voxels. Of course, \nthis is nowhere close to an end all be all;   there are huge improvement possibilities to both \nthe data structure and the algorithm. For example,   what if we could compress the data so that more \nvoxels can fit in less memory? Or what if we knew   that larger regions of the grid were completely \nempty meaning that we could step through them in   their entirety all in one single iteration? \nBut I'll leave that for the next video where   I'll be covering sparse voxel octrees and an \nimproved voxel ray traversal algorithm that   takes advantage of the sparseness. Thank you \nfor watching, and till next time — goodbye.",
  "timestamped_text": "0:00 Today we're reinventing voxel ray traversal from \nscratch to be able to render over 32 billion  \n0:06 voxels in real time. Most voxel games, like \nMinecraft, convert their worlds into triangle  \n0:12 meshes to render them. To me, this has always \nfelt like a compromise: we waste time, memory,  \n0:18 and complexity converting perfectly good voxel \ndata into triangles because GPUs are optimized  \n0:24 for triangle rasterization. But what if we \ncould render the world directly — no triangles,  \n0:30 no meshing — and somehow do it fast enough for \nreal time? That's exactly what we'll do today.  \n0:38 We’ll go from the basics of what a voxel even is \nto an algorithm capable of rendering billions of  \n0:43 voxels in real time by casting a ray for each \npixel on the screen using the GPU. And towards  \n0:49 the end of the video, I'll show you one simple \noptimization technique that 10x'd the FPS. So,  \n0:56 how do we go from a bunch of voxels and a camera \nto pixels on a screen? Well, to answer that,  \n1:03 we need to start at the very beginning — \"What is \na Voxel?\" The screen you're watching this video on  \n1:10 is made up of millions of physical pixels, behind \neach of which is a logical pixel — a simple piece  \n1:17 of data that determines its color. This is \noften an RGB color but it doesn't have to be;  \n1:25 it could just as well be a single luminance value \nor literally anything else, and it would still be  \n1:31 a pixel. In my opinion, the only thing required \nfor something to be a pixel is that it's a small  \n1:38 building block of 2D space. In the same way we \ncan imagine 3D space as being made up of so-called  \n1:46 voxels. For simplicity, my voxels will each be a \nsingle bit of data: on or off, one or zero, true  \n1:55 or false, solid or empty. But, like pixels, voxels \ncan store any kind of data you want. For example,  \n2:04 each voxel could hold an integer representing \na material type like air, stone, grass, dirt,  \n2:10 etc., etc. But the idea of a grid of voxels itself \ndoesn't help us a lot unless we can efficiently  \n2:19 read and write it to memory. Therefore, we’ll \nuse this flattening function to convert a 3D  \n2:25 coordinate to a 1D index, allowing us to \nstore the voxel data in an array of size  \n2:31 width times depth times height. You can think \nof this function as taking each layer along  \n2:37 the y-axis and placing them side by side along the \nx-axis, and then taking each row along the z-axis  \n2:44 and placing them end to end. There are other \nmethods that preserve spatial locality better to  \n2:50 improve memory performance, or ones that compress \nthe voxel data to use less memory overall,  \n2:56 but I'll talk more about those towards the end of \nthe video. Going forward, the visualizations will  \n3:02 be in 2D and the math and algorithms will be in \n3D but all of it works in any dimension you want  \n3:09 with only a few minor modifications. Finally, we \nneed to be able to convert an arbitrary position  \n3:15 in space to the corresponding voxel coordinates \nand for that we’ll just take the floor of each  \n3:21 component. If this position has the possibility of \nbeing outside of the grid, due to numerical errors  \n3:27 or actually being outside, we'll have to clamp it \nor check it to make sure we don't access out of  \n3:33 bounds. Now that we know what a voxel is, how do \nwe describe the idea of a pixel looking at one?  \n3:41 That’s where the mathematical concept of a ray \ncomes in — which leads us to the next question:  \n3:48 \"What is a Ray?\" In our case it will make the \nmost sense to define a ray, given by the origin  \n3:56 O and the non-zero direction D, as the set of all \npoints P that satisfy these two constraints. In  \n4:04 other words, all points from the given origin \nalong the given direction. The variable t is  \n4:11 a parameter that tells us how many multiples \nof the direction we've moved from the origin.  \n4:16 Note that nowhere in this definition do we say \nthat the direction is normalized. It can be  \n4:21 whatever length you want as long as it's not zero. \nSo given a ray corresponding to some pixel we need  \n4:30 to figure out if it hits a voxel — and if so, \nwhich one. The idea is to walk through the voxel  \n4:39 grid along that ray, in increasing values of t, \nuntil we either find the first solid one or exit  \n4:45 the bounds of the grid. This is what’s known \nas voxel ray traversal. Now that we know the  \n4:52 idea behind what voxel ray traversal is, we can \nstart to think about how we would implement it.  \n4:58 Let’s begin with a naive method, because every \ngood computer science problem starts with  \n5:04 something painfully slow and completely wrong. In \nthis case, that would be to just start at the ray  \n5:13 origin stepping forward in small delta t multiples \nof the direction, checking for a solid voxel at  \n5:19 each step. This works but it has one major issue; \nit can completely miss voxels which the ray should  \n5:29 intersect with. And if we try to minimize the \nrisk of this by decreasing the step size, we  \n5:35 could spend hundreds or thousands of iterations on \na single voxel which is very slow, not to mention  \n5:43 that it doesn't even fix the issue. So let's take \na step back and think about the problem again.  \n5:50 We have a grid and a ray and we want to determine \nwhich voxels the ray intersects with in order. So  \n6:00 let's start simple; how do we find the first \nvoxel? If the origin is inside of the grid, we  \n6:07 know that the first voxel is just the floor of the \nray origin, but if it's outside, we somehow need  \n6:14 to calculate where the ray enters the axis-aligned \nbounding box around the grid. For this we'll need  \n6:22 a Ray-AABB intersection algorithm. To do this we \nwill take the mathematical definition of a ray and  \n6:29 the mathematical definition of an AABB and solve \nfor the ray parameter t. After working through  \n6:36 the math and handling some edge-cases we arrive \nat this algorithm, known as the slab method for  \n6:43 Ray-AABB intersection. Now, because it's almost \nalways faster to multiply than to divide, it makes  \n6:50 sense to precompute one over the direction and \nstore that in our ray struct. And then multiply  \n6:56 by that instead of divide by the direction. From \nthe Ray-AABB intersection algorithm we get the  \n7:03 values of t when the ray enters and exits the \nbounding box. If we don't hit the bounding box,  \n7:10 we know that we won't hit a single voxel in the \ngrid either so we can return early. Otherwise we  \n7:17 use the first t-value with the ray definition to \ncalculate the point of entry. Since this point has  \n7:23 the chance of being slightly outside of the grid \ndue to floating-point errors, we'll need to use  \n7:28 the clamping version when we get the corresponding \nvoxel coordinate. Now that we know how to get the  \n7:34 first voxel that the ray intersects with, the only \nthing we need for a complete voxel ray traversal  \n7:39 algorithm is a way to get the next voxel since \nthen we can just do that in a loop until we hit  \n7:45 a solid voxel or go out of bounds. So given a ray \nwhich we know intersects with a given voxel, how  \n7:54 do we get the next voxel that the ray intersects \nwith? Well, we can imagine the voxel grid as being  \n8:01 made up of a stack of axis-aligned planes along \neach axis. The ray travels through these planes,  \n8:09 and whenever it crosses one of them, we enter \na new voxel. So, to find the next voxel,  \n8:16 all we need to do is figure out which of these \nplanes the ray will exit the voxel through. The  \n8:22 lesser plane on each axis, just has the positions \nof the components of v and the greater planes,  \n8:29 the components of v plus one. First of all, we can \nnotice that if the x-component of the direction  \n8:35 is positive we can never exit through the lesser \nx-plane, and if it's negative we can never exit  \n8:42 through the greater x-plane. This applies to y and \nz too which brings us down to one plane per axis  \n8:49 which we can determine the positions of based on \nthe sign of the direction. Because of some quirks  \n8:55 of floating-point zero we'll actually want to use \nthe sign of the inverse of the direction instead  \n9:00 of the sign of the direction directly. Now that \nwe know the position of the planes, we just need  \n9:06 to calculate which plane the ray intersects with \nfirst. So let's focus on the x-plane. Since the  \n9:14 x-plane is defined as being all points for \nwhich the x-coordinate is a certain value,  \n9:19 let's call it x_0, and the ray is defined as all \npoints that satisfy this equation, we just have to  \n9:26 solve for the value of t when the x-component of \nP is equal to x_0. This is just a linear equation  \n9:34 and by solving it we get this, and I'll call it \nt_x because it's the t-value to hit the x-plane.  \n9:42 Of course, we could just as well have done this \nfor the y-axis or the z-axis and the derivation  \n9:48 would've been the same. Now that we have the \nthree t-values, we just have to compare them to  \n9:53 see which one occurs first. From the sign of the \ndirection, we also know if we're supposed to step  \n9:59 in the positive or negative direction, in other \nwords, to get the next voxel in the traversal  \n10:04 we want to add the sign of the direction to the \ncomponent of the voxel coordinate, for which the  \n10:09 plane intersection t-value is lowest. And again \nfor the same reason, we'll use the sign of the  \n10:15 inverse instead. Putting it all together, we first \ncalculate the initial voxel using the Ray-AABB  \n10:21 intersection algorithm. Then we repeatedly step \nto the next voxel by comparing the t-values for  \n10:27 the plane intersections and if we continue this \nprocess until we hit a solid voxel or exit the  \n10:33 bounds of the grid, we will have traversed every \nintersected voxel. One quick improvement. We can  \n10:40 see that each iteration, coord is increased by \nthe sign of the direction which means that t  \n10:45 is increased by the sign of the direction times \nthe inverse of the direction, in other words the  \n10:51 absolute value of the inverse of the direction. \nTherefore, we can pull out the entirety of t and  \n10:58 increment it each iteration instead. The algorithm \nthat we now have is essentially the one described  \n11:04 by Amanatides and Woo in their 1987 paper 'A Fast \nVoxel Traversal Algorithm for Ray Tracing'. Now  \n11:14 let's use the algorithm to render something and \nfor that, we of course need something to render.  \n11:20 Therefore, I've chosen the Stanford Bunny, a model \nwhich is very popular in computer graphics. The  \n11:27 Stanford Bunny is a triangle mesh so we'll need to \nvoxelize it which I won't cover how to do in this  \n11:33 video but the algorithm is included in the GitHub \nrepository in the description below along with a  \n11:38 bare-bones implementation of this video's topic in \nRust and Vulkan. And now if we set up a camera and  \n11:44 shoot a ray for each pixel using a compute shader \nrunning the algorithm, we get this. Right now,  \n11:51 the traversal only tells us whether we miss or \nwhich voxel we hit. But with a bit of extra math,  \n11:58 we can efficiently compute which face of the voxel \nwe hit, where on the face we hit, and how far from  \n12:04 the camera it happened. This gives us everything \nwe need for basic shading and texturing, or even  \n12:10 launching secondary rays for ray tracing. Now it's \ntime for that one optimization that I alluded to  \n12:16 at the start of the video. Currently, at a grid \nresolution of 2048 cubed and a screen resolution  \n12:23 of 1000 by 1000 we're getting about 12 FPS. \nAfter this optimizations, we'll hit roughly 121  \n12:32 FPS — which is a 10x speedup. But before we get to \nthat, I want to cover an alternative optimization  \n12:40 which is almost as good but way easier to explain. \nIn the \"What is a Voxel?\" section of this video,  \n12:48 we covered the flattening function and I \nmentioned that there are other ways of doing it.  \n12:53 It turns out that if we switch to this one, \nknown as a Z-order curve, we get 102 FPS which  \n13:01 is already over 8 times faster. You might be \nthinking, \"Wait, we just made the flattening  \n13:08 function way more complicated, and it got faster?\" \nYeah! The reason why is that with a Z-order curve,  \n13:17 we place voxels that are nearby in space nearby \nin memory. This has two main implications on the  \n13:24 GPU. First of all, since our voxel ray traversal \nalways steps from one voxel to a neighboring one,  \n13:30 this improves the likelihood that the next voxel \nis already in the cache. And second of all, it  \n13:36 improves the likely hood that two nearby rays want \nto access the same memory meaning that the GPU  \n13:42 doesn't have to read as much data into cache. All \nin all, this means we spend less time waiting for  \n13:48 memory to read and more time actually traversing. \nNow like I said before, there's actually a better  \n13:55 way to do this. Since we're doing the traversal on \nthe GPU, we have access to GPU specific structures  \n14:01 like 3D textures which are heavily optimized for \ndata locality using way more sophisticated methods  \n14:07 than Z-order curves. Switching to storing our \nvoxels in a 3D image brings us to 121 FPS which is  \n14:16 a 10 times improvement over the original 12. So by \nrethinking how the voxel data is stored in memory,  \n14:23 first with a Z-order curve and then a 3D texture, \nwe brought our FPS from 12 to 121 without even  \n14:32 changing the algorithm itself. So there you \nhave it, from the definitions of voxels,  \n14:37 rays and traversals, to a complete real-time \nrendering pipeline with zero triangles, capable  \n14:44 of rendering billions of voxels. Of course, \nthis is nowhere close to an end all be all;  \n14:51 there are huge improvement possibilities to both \nthe data structure and the algorithm. For example,  \n14:58 what if we could compress the data so that more \nvoxels can fit in less memory? Or what if we knew  \n15:03 that larger regions of the grid were completely \nempty meaning that we could step through them in  \n15:08 their entirety all in one single iteration? \nBut I'll leave that for the next video where  \n15:15 I'll be covering sparse voxel octrees and an \nimproved voxel ray traversal algorithm that  \n15:20 takes advantage of the sparseness. Thank you \nfor watching, and till next time — goodbye."
}
