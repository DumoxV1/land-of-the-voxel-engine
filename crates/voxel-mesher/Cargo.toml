[package]
name = "voxel-mesher"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
voxel-core = { path = "../voxel-core" }

[dev-dependencies]

[lib]
path = "src/lib.rs"
